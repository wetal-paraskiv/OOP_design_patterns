package creational_design_patterns.singleton_dp;

public class MyThread implements Runnable{
    @Override
    public void run() {
//        System.out.println("Thread Nr: " + Thread.currentThread().getId() + " started...");

        // thread safe problems
//        System.out.println(SingleTonLazyInit.getInstance().hashCode());

//        System.out.println(SingleTonLazyInitHolderIdiom.getInstance().hashCode());

//        System.out.println(SingleTonLazyInitSynchronized.getInstance().hashCode());
//        System.out.println(SingleTonLazyInitSynchronized.getInstanceUsingDoubleLocking().hashCode());

//        System.out.println(SingleTonEagerInit.getInstance().hashCode());
//        System.out.println(SingleTonEagerInitStatic.getInstance().hashCode());

//        System.out.println(SingleTonBestPractice.getInstance().hashCode());
    }
}
